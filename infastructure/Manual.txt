1.First run terraform init,validate, apply -auto-approve in 01-ekscluster-terraform manifests folder


2.# Configure kubeconfig for kubectl
aws eks --region <region-code> update-kubeconfig --name <cluster_name>

#Currents setup
aws eks --region us-east-1 update-kubeconfig --name DevOps-dev-django-eks-cluster

3.Run terraform  init,validate, apply -auto-approve in 02-k8resources-terraform-manifests

4.Checking

# List Nodes
kubectl get nodes -o wide

# List Pods
kubectl get pods -o wide

# List Services
kubectl get svc
kubectl get svc -o wide


# Access Sample Application on Browser
http://<CLB-DNS-NAME>
http://<NLB-DNS-NAME>

